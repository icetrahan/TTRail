import 'package:flutter/material.dart';

class NewInsp extends StatelessWidget {
  const NewInsp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
